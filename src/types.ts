export type Site = {
  NAME: string;
  EMAIL: string;
  AUTHOR: string;
  IMAGE: string;
  "DESCRIPTION-es": string;
  "DESCRIPTION-en": string;
};

export type Metadata = {
  TITLE: string;
  DESCRIPTION: string;
};

export type Socials = {
  NAME: string;
  HREF: string;
}[];
